package cs3500.pa03.view;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
/*
class WriterImplTest {

  private Appendable appendable;
  private Writer writer;
  private static final String VALUE = "input"; // this is a constant whicb will not be changed (static and written in UPPER_SNAKE_CASE)

  @BeforeEach
  public void setUp() {
    this.appendable = new StringBuilder();
    this.writer = new BattleSalvoCommandView(,this.appendable);
  }

  @Test
  public void testSuccess() {
    // check empty StringBuilder
    assertEquals("", this.appendable.toString());

    // write to it
    this.writer.write(VALUE);

    // check only that value appears in the StringBuilder
    assertEquals(VALUE, this.appendable.toString());
  }

  @Test
  public void testFailure() {
    this.writer = new WriterImpl(new MockAppendable());
    Exception exc = assertThrows(RuntimeException.class, () -> this.writer.write(VALUE), "Mock throwing an error");
    assertEquals("Mock throwing an error", exc.getMessage());
  }


}

 */
